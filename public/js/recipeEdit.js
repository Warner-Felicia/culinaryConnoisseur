const addIngredientButton = document.querySelectorAll('.addIngredientButton');
const addDirectionButton = document.querySelectorAll('.addDirectionButton');
const addTagButton = document.querySelectorAll('.addTagButton');

addIngredientButton.forEach(btn => {
    btn.addEventListener('click', addIngredient);
});

addDirectionButton.forEach(btn => {
    btn.addEventListener('click', addDirection);
});

addTagButton.forEach(btn => {
    btn.addEventListener('click', addTag);
});

const addIngredient = () => {
  
}

const addDirection = () => {

}

const addTag = () => {

}