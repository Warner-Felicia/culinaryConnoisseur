<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Culinary Connoisseur</title>
    <link href="/css/login.css" rel="stylesheet" type="text/css">
</head>

<header>
  <button id="side-menu-toggle">Menu</button>
  <div class="nav_container">
    <nav class="main-header_nav">
      <ul>
          <a href="/"><img src="../../images/CulinaryLogo.png" alt="logo" class="logo"></a>
      </ul>
    </nav>
  </div>
</header>

<body>
    <!-- sign in portion -->
  <div class="container" id="container">
    <div class="form-container sign-up-container">
      <form action="/signUp" method="POST">
        <h1>Create Account</h1>
        <% if (errorMessage) { %>
          <p class='user-message'><%= errorMessage %></p>
        <% } %>
        <input name="firstName" type="text" placeholder="First Name" value="<% if (hasError) { %><%=user.firstName%><% } %>" required/>
        <input name="lastName" type="text" placeholder="Last Name" value="<% if (hasError) { %><%=user.lastName%><% } %>" required/>
        <input name="email" type="email" placeholder="Email" value="<% if (hasError) { %><%=user.email%><% } %>" required/>
        <input name="password" type="password" placeholder="Password" required/>
        <input name="securityPhrase" type="text" placeholder="Security Phrase" value="<% if (hasError) { %><%=user.securityPhrase %><% } %>" required/>
        <input name="passwordHint" type="text" placeholder="Security Phrase Hint" value="<% if (hasError) { %><%=user.passwordHint%><% } %>" required/>
        <input type="hidden" name="_csrf" value="<%= csrfToken %>">
        <button>Sign Up</button>
      </form>
    </div>
    <div class="form-container sign-in-container">
      <!-- <form action="#"> -->
        <form action="/signIn" method="POST">
        <h1>Sign in</h1>
        <% if (errorMessage2) { %>
          <p class='user-message'><%= errorMessage2 %></p>
        <% } %>
        <input name="email" type="email" placeholder="Email" value="<% if (hasError) { %><%=user.email%><% } %>" required/>
        <input name="password" type="password" id='password' name="password" placeholder="Password" required/>
        <div class='passwordToggle'>Show Password</div>
        <a style="font-size: 15px;" action="/reset" href="/reset">Forgot your password?</a>
        <input type="hidden" name="_csrf" value="<%= csrfToken %>">
        <button class="button">Sign In</button>
      </form>
    </div>
    <!-- sign up portion -->
    <div class="overlay-container">
      <div class="overlay">
        <div class="overlay-panel overlay-left">
          <h1>Welcome Back!</h1>
          <p>To keep track of some great recipes please login with your personal info</p>
          <button class="ghost" id="signIn">Sign In</button>
        </div>
        <div class="overlay-panel overlay-right">
          <h1>Hello, Friend!</h1>
          <p style="font-weight: 2px;">Enter your personal details and start cooking with us</p>
          <button class="ghost" id="signUp">Sign Up</button>
        </div>
      </div>
    </div>
  </div>

  <footer>Culinary Connoisseur</footer>
  <script src='/js/login.js'></script>
  <script src='/passwordToggle.js'></script>
</body>
</html>